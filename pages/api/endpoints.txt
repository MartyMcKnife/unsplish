POST /photo - adds a new url, label and id to database
DELETE /photo/[id] - removes corresponding id from database
GET /photo/[user] - gets all photos for user (sends back label, url and id) 
GET /photo/[label] - gets a list of photos for corresponding label
 (User will be a uuid stored in local storage)